// client.ejs
const weather = `https://api.open-meteo.com/v1/forecast?latitude=${latitude}&longitude=${longitude}&hourly=temperature_2m&current_weather=true`;
// Rest of the client-side JavaScript code

console.log(weather)
